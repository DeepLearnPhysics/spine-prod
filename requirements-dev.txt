# Development dependencies for spine-prod
# Install with: pip install -r requirements-dev.txt
# Aligned with SPINE repository standards

# Base requirements
jinja2>=3.0.0
pyyaml>=5.0.0

# Note: SPINE is provided via git submodule (see spine/)
# Tests use the submodule version via PYTHONPATH

# Testing
pytest>=6.0.0
pytest-cov>=2.10.0

# Code formatting and linting
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# Pre-commit hooks
pre-commit>=3.0.0

# YAML linting (spine-prod specific)
yamllint>=1.35
