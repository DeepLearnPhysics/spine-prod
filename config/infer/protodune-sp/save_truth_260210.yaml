# ProtoDUNE-SP truth saving configuration
# Revision date: February 10, 2026

include:
  - base/base_260210.yaml
  - io/io_260210.yaml

# Overrides
override:
  # Only build truth objects
  build.mode: truth

  # No reconstructed information to write
  io.writer.keys-: [points, depositions, reco_particles, reco_interactions]
