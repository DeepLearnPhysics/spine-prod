# ProtoDUNE-SP model trained on the mpvmpr v1 dataset
# Revision date: September 6, 2026

__meta__:
  kind: bundle
  version: "260906"
  date: "2026-09-06"
  description: "ProtoDUNE-SP collection-only model trained on mpvmpr v1"
  compatible_with:
    base: ">=260210"
    io: ">=260210"
  tags:
    [
      collection-only,
      calibrated,
      deghosting,
      ppn-restrict,
      grappa-use-closest,
      grouping-through-track,
    ]
  training_set: "mpvmpr_v1"

include: model/protodune-sp/full_chain/model_260906.yaml

override:
  # Set this to the published full-chain checkpoint after review.
  model.weight_path: null
