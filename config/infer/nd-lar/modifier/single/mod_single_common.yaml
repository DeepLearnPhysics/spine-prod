# Single-module modification for ND-LAr configs
# Include this after the main config to process only a single module

override:
  # Change the detector geometry to single-module
  geo.detector: fsd
  geo.tag: cr3

  # Change the batch size to a much larger number
  io.loader.batch_size: 1024
