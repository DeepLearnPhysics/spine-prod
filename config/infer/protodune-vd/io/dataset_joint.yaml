__meta__:
  kind: fragment
  description: protodune-vd LArCV dataset restricted to joint-overlay-compatible products

include: dataset_common.yaml

remove:
  - schema.run_info
  - schema.flashes
  - schema.crthits
