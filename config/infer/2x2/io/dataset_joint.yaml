__meta__:
  kind: fragment
  description: 2x2 LArCV dataset restricted to joint-overlay-compatible products

include: dataset_common.yaml

remove:
  - schema.run_info
  - schema.trigger
  - schema.flashes
