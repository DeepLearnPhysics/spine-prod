# Base configuration for ICARUS detector with collection-only charge rescaling
# Common settings shared across all ICARUS full chain configs
# Include this file and override specific settings as needed
include:
  - icarus_base.cfg

override:
  # Change charge rescaling to collection-only
  model.modules.chain.charge_rescaling: collection
