# Generic truth conversion configuration
# Revision date: August 5, 2024

__meta__:
  kind: bundle
  version: "240805"
  date: "2024-08-05"
  description: "Generic LArCV-to-SPINE truth conversion configuration"

include:
  - infer/generic/base/base_240718.yaml
  - infer/generic/io/io_240805.yaml

# Overrides
override:
  # Only build truth objects
  build.mode: truth

  # Force CPU execution for truth-only parsing
  base.world_size: 0

  # No reconstructed information to write
  io.writer.keys-: [points, depositions, reco_particles, reco_interactions]
