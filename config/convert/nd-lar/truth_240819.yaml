# ND-LAr truth conversion configuration
# Revision date: August 19, 2024

__meta__:
  kind: bundle
  version: "240819"
  date: "2024-08-19"
  description: "ND-LAr LArCV-to-SPINE truth conversion configuration"

include:
  - infer/nd-lar/base/base_240819.yaml
  - infer/nd-lar/io/io_240819.yaml

# Overrides
override:
  # Only build truth objects
  build.mode: truth

  # Force CPU execution for truth-only parsing
  base.world_size: 0

  # No reconstructed information to write
  io.writer.keys-: [points, depositions, reco_particles, reco_interactions]
