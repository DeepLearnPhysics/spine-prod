__meta__:
  kind: fragment
  description: SPINE v1 standalone UResNet deghosting model

model:
  name: uresnet
  weight_path: null

  network_input:
    data: data

  loss_input:
    seg_label: seg_label

  modules:
    uresnet: !include model/common/uresnet_deghost/network_v1.yaml
    uresnet_loss: !include model/common/uresnet_deghost/loss_v1.yaml
